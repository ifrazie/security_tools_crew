info_task:
  description: >
    Conduct a port scan of {ip_address}
    Output should be in JSON format with detailed port information.
  expected_output: >
    JSON formatted results of the Nmap scan showing ports, services, and risk levels.
  agent: cybersecurity_analyst
  output_file: output/nmap_scan.json
  output_format: json